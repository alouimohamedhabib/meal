import { CleanCategorieNamesPipe } from './clean-categorie-names.pipe';

describe('CleanCategorieNamesPipe', () => {
  it('create an instance', () => {
    const pipe = new CleanCategorieNamesPipe();
    expect(pipe).toBeTruthy();
  });
});
